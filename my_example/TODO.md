# TODO（my_example）

本文档只保留当前项目最值得继续推进的事项，重点服务两件事：

1. 提高实验结论的可信度。
2. 让论文与答辩表述更完整、更一致。

## 1. 当前优先事项

| 优先级 | 事项 | 现状 | 建议 |
| --- | --- | --- | --- |
| P0 | 多 seed 统计 | 多数结论仍来自单次运行 | 对 `default + all` 先补 5 到 10 个 seed，报告 `mean ± std` |
| P0 | 指标解释 | `fitness` 与 `M1/M2/M3` 不完全一致 | 在论文中明确区分“内部优化目标”和“外部报告指标” |
| P0 | 时间冲突场景 | 当前多数配置下 M1 区分度偏弱 | 新增一组时间窗更密、冲突更强的场景 |
| P0 | 复现说明 | 实验记录还不够统一 | 固定配置文件、随机种子、代码版本和运行环境 |
| P1 | GA 早停 | `large` 场景耗时过高 | 增加早停阈值或时间预算 |
| P1 | 收敛记录 | 目前主要依赖控制台输出 | 保存每代 `best / avg / std fitness` |
| P1 | 超参数分析 | 默认参数已有经验值 | 选取少量参数做敏感性分析 |
| P1 | 模型边界说明 | 工程假设分散在各文档中 | 单独整理“假设与局限”小节 |
| P2 | 图表统一 | 文档风格已基本成型 | 统一术语、表头和图注口径 |
| P2 | 可选扩展 | 当前主线已经足够 | 仅在时间充裕时再考虑更强对比算法 |

## 2. 最建议先完成的 5 件事

1. 跑完 `default` 场景下五算法的多 seed 主实验。
2. 在报告中明确解释 `fitness` 与 `M1/M2/M3` 的关系。
3. 给 GA 增加早停或时间预算，控制大规模场景耗时。
4. 设计一组“时间窗冲突主导”的新配置。
5. 统一论文中的实验假设、局限和复现说明。

## 3. 当前结论边界

- 现有项目已经足以支撑毕业设计主体。
- 当前最需要补的不是新算法，而是统计性和解释性。
- 后续开发应优先服务论文可信度，而不是继续扩展功能数量。
